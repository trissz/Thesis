const paginationDataset = {
    defaultCurrentPage: 1,
    defaultItemsPerPage: 10,
};

const selectBasedTaskDataset = {
    defaultMinSelections: 1,
    defaultMaxSelections: 1,
}

const actionMethods = {
    CREATE: "create",
    UPDATE: "update",
    DELETE: "delete",
    VIEW: "view",
};

const taskTypes = {
    SORT_BASED: "Sort based",
    SELECT_BASED: "Select based",
    INPUT_BASED: "Input based",
};